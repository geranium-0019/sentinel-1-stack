# 実行時のアンラップ並列数（2026-09-20）

- `run --unwrap-jobs N` をホストCLI・コンテナCLIに追加。既定1。
- 生成済みrun_filesと設定のfingerprintは変更せず、unwrap工程の各グループを実行時に分割。
- 生成されたwait境界、他工程の並列数、個別終了コード検査、失敗時の兄弟プロセス停止を維持。
- 実行記録に上限値を記録し、成功した再開では以前のerror欄を削除。
- -9/137ではSIGKILL・OOMの可能性、確認先、1並列での再開例を日本語表示。OOMと断定しない。

## 検証

- runner 10テスト成功。実プロセスのSIGKILL、兄弟停止、完了工程スキップ、上限2から1への変更再開を含む。
- 全110テスト成功（既存Docker環境に現在のソースを読み取り専用マウント）。
- `sentinel-1-stack:dev` をビルドし、ホスト・コンテナのrun --helpで新オプション確認。
- イメージID: sha256:e6e36904d8d0c36427718a57a2426014ed8c815995c880ca970076a332619ba5
- Anakの設定・手編集済みrun_files・実行記録・成果物は変更していない。
- 実データでのアンラップ完走、既知の環境の不規則なエラー解明、正式版配布は未完了。
